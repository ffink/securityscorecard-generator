{
  "name": "securityscorecard-generator",
  "version": "1.0.0",
  "description": "Get scorecard data from SecurityScorecard",
  "main": "src/main.js",
  "scripts": {
    "lint": "eslint 'src/**.js' --fix",
    "test": "eslint 'src/**.js'",
    "build": "ncc build src/main.js",
    "precommit": "npm run build && git add dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ffink/securityscorecard-generator"
  },
  "keywords": [
    "actions",
    "node"
  ],
  "author": "ffink",
  "license": "GNU",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "@actions/exec": "^1.0.0",
    "@actions/github": "^1.0.0",
    "axios": "^0.19.1"
  }
}
